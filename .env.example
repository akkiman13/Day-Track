# Optional personal access gate. Keep these server-side; never use NEXT_PUBLIC_ prefixes.
STUDYTRACK_PASSWORD=replace-with-a-strong-private-password
STUDYTRACK_AUTH_SECRET=replace-with-at-least-32-random-bytes
